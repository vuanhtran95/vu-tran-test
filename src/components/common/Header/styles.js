import styled from "styled-components";
import { SPACING } from "../../../shared";

export const HeaderWrapper = styled.div`
  display: flex;
  justify-content: end;
  align-items: center;
  gap: ${SPACING.S};
`;
